import { UpdateOptions } from "../../core";
import { AnyObject } from "../../types";
/** Multiply the value of a field by a number. */
export declare const $mul: (obj: AnyObject, expr: Record<string, number>, arrayFilters?: AnyObject[], options?: UpdateOptions) => string[];
