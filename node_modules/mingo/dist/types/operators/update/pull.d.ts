import { UpdateOptions } from "../../core";
import { AnyObject } from "../../types";
/** Removes from an existing array all instances of a value or values that match a specified condition. */
export declare const $pull: (obj: AnyObject, expr: AnyObject, arrayFilters?: AnyObject[], options?: UpdateOptions) => string[];
