import { UpdateOptions } from "../../core";
import { AnyObject } from "../../types";
/** Appends a specified value to an array. */
export declare const $push: (obj: AnyObject, expr: AnyObject, arrayFilters?: AnyObject[], options?: UpdateOptions) => string[];
